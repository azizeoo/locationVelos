CREATE TEXT TABLE VELO(
COD_VELO VARCHAR(10) NOT NULL ,
MARQUE VARCHAR(30),
PRIX DOUBLE,
COD_CLIENT VARCHAR(10) NOT NULL ,
PRIMARY KEY (COD_VELO),
FOREIGN KEY (COD_CLIENT) REFERENCES CLIENT(COD_CLIENT)
);

SET TABLE VELO SOURCE "velo;fs=|"

CREATE TEXT TABLE CLIENT(
COD_CLIENT VARCHAR(10) NOT NULL ,
NOM VARCHAR(30) NOT NULL,
PRENOM VARCHAR(30) NOT NULL,
PRIMARY KEY (COD_CLIENT)
);

SET TABLE CLIENT SOURCE "client;fs=|"

INSERT INTO CLIENT VALUES('99999','nom test','prenom test');
INSERT INTO VELO VALUES('99999','marque test',10000,'99999');